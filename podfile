# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FMDB' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
   pod 'FMDB'
   pod 'SDAutoLayout'
  # Pods for FMDB

  target 'FMDBTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FMDBUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
